# ENX03M — Geant4 Simulation Platform

**ENX03M** — це симуляційний проєкт на Geant4, що підтримує кілька задач (геометрій) з автоматичним аналізом, візуалізацією та збором спектральних даних. Все реалізовано у вигляді одного `.exe` з інтерактивним вибором задачі.

---

## 🔧 Запуск

При старті:
програма пропонує обрати одну з задач:


Відповідно до вибору:
- підвантажується геометрія сцени;
- виконується відповідний макрофайл (`.mac`);
- відкривається візуальна сцена;
- після `beamOn` дані зберігаються у `.txt` та `.csv`;
- виводиться спектр у термінал.

---

## 📁 Задачі

### 1. `tungsten` — Тонка вольфрамова мішень
- Геометрія: 93×55×2 мм
- Вхідний пучок e⁻ 8.7 МеВ
- Детектор після мішені (γ, e⁻, n)
- Макрофайл: `tungsten.mac`
- Файл аналізу: `tungsten_spectrum.csv`

### 2. `betargets` — Блоки з берилію
- Вольфрамова мішень + 2 блоки Be (7 см)
- Детектори:
  - на виході з Be2
  - бічна поверхня Be1
  - бічна поверхня Be2
- Реєструються: γ, e⁻, n, α
- Макрос: `betargets.mac`
- CSV: `betargets_spectrum.csv`

### 3. `M-10` — Мікротрон М-10
- Електронний пучок 8.7 МеВ
- Мішень: 93×55×2 мм
- Детектори:
  - D1 — на 10 см (по Z)
  - D2 — на 20 см (по Z)
  - D3 — під кутом 45°
- Макрос: `M-10.mac`
- CSV: `microtron10_spectrum.csv`

---

## 📊 Аналіз

Використовуються два канали одночасно:

- **Текстовий вивід (`.txt`)** — генерується кожним `SensitiveDetector` (`SpectrumSD::Save()`)
- **Аналізменеджер (`G4AnalysisManager`)** — створює `.csv`:
  - γ, n, e⁻, α (по сенсорам)
  - готові для побудови графіків

> Усі результати автоматично генеруються після `/run/beamOn`.

---

## 📦 Структура макросів

Кожен макрофайл виконує:
```plaintext
/run/initialize
/vis/open
/vis/scene/create
/vis/drawVolume
/run/beamOn 1000
/analysis/write
/analysis/close

🧩 Розширення
Нові задачі додаються через:

новий клас Detector_*

запис у SelectTask()

відповідний .mac

Аналіз — розширюється через CreateH1(...) + FillH1(...)

✅ Залежності
Geant4

Qt (для візуалізації)

C++17+


---

🔚 Можна адаптувати це для публікації, дипломної роботи як документацію до проєкту. 
автогенерацію графіків із CSV (Gnuplot або Python).